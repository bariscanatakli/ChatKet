# Database
POSTGRES_USER=chatket
POSTGRES_PASSWORD=chatket_secret
POSTGRES_DB=chatket

# JWT (change in production!)
JWT_SECRET=your_super_secret_jwt_key_change_me

# Server
NODE_ENV=production

# Exposed port (default: 8080)
PORT=8080
